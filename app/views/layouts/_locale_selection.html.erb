<%= LOCALES.map { |locale| 
  if current_locale && (locale == current_locale)
      "<b>#{t("locale_"+locale.to_s)}</b>"
  else
    link_to_remote(
      t("locale_"+locale.to_s),
      :url => set_locale_path(locale)
    )
  end
  }.join(NBSP+"|"+NBSP)
-%>